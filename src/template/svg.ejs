'use client'
// This file is generated by image-to-react-component automatically
// DO NOT CHANGE IT MANUALLY!
import Icon from '@ant-design/icons';
import React from 'react';

// 导入SVG图标
import <%= componentName %>Svg from '<%= relativeFilePath %>';

export interface <%= componentName %>SvgProps {
  /** 自定义类名 */
  className?: string;
  /** 图标大小 */
  size?: number | string;
  fontSize?: number | string;
  /** 图标颜色 */
  color?: string;
  /** 点击事件 */
  onClick?: () => void;
  /** 样式 */
  style?: React.CSSProperties;
}

export const <%= componentName %>SvgIcon: React.FC<<%= componentName %>SvgProps> = ({
  className,
  size,
  fontSize,
  color,
  onClick,
  style,
}) => {
  // 合并样式
  const combinedStyle: React.CSSProperties = {
    ...style,
  };

  if (size) {
    combinedStyle.fontSize = typeof size === 'number' ? `${size}px` : size;
  }

  if(fontSize) {
    combinedStyle.fontSize = typeof fontSize === 'number' ? `${fontSize}px` : fontSize;
  }

  if (color) {
    combinedStyle.color = color;
  }

  return (
    <Icon
      component={<%= componentName %>Svg}
      className={className}
      style={combinedStyle}
      onClick={onClick}
    />
  );
};

export default <%= componentName %>SvgIcon;