'use client'
// This file is generated by image-to-react-component automatically
// DO NOT CHANGE IT MANUALLY!
import Icon from '@ant-design/icons';
import React from 'react';

// 导入SVG图标
import <%= componentName %>Svg from '<%= relativeFilePath %>';

// 图标映射表
const iconMap = {
  '<%= iconName %>': <%= componentName %>Svg,
} as const;

// SVG图标名称类型
export type SvgIconName = keyof typeof iconMap;

export interface <%= componentName %>SvgProps {
  /** 图标名称 */
  name: SvgIconName;
  /** 自定义类名 */
  className?: string;
  /** 图标大小 */
  size?: number | string;
  /** 图标颜色 */
  color?: string;
  /** 点击事件 */
  onClick?: () => void;
  /** 样式 */
  style?: React.CSSProperties;
}

const <%= componentName %>SvgIcon: React.FC<<%= componentName %>SvgProps> = ({
  name,
  className,
  size,
  color,
  onClick,
  style,
}) => {
  const SvgComponent = iconMap[name];

  // 合并样式
  const combinedStyle: React.CSSProperties = {
    ...style,
  };

  if (size) {
    combinedStyle.fontSize = typeof size === 'number' ? `${size}px` : size;
  }

  if (color) {
    combinedStyle.color = color;
  }

  return (
    <Icon
      component={SvgComponent}
      className={className}
      style={combinedStyle}
      onClick={onClick}
    />
  );
};

export default <%= componentName %>SvgIcon;