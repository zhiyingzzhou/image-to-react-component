'use client'
// This file is generated by image-to-react-component automatically
// DO NOT CHANGE IT MANUALLY!
import { ComponentProps } from 'react';
import <%= componentName %> from '<%= relativeFilePath %>?react';

type SvgProps = ComponentProps<'svg'> & { title?: string };

interface SvgIconCompProps extends SvgProps {
}

export const <%= componentName %>Svg = (props: SvgIconCompProps) => {
  return (
    <<%= componentName %>
      {...props}
      fill={props.fill || 'currentColor'}
      viewBox={props.viewBox || '<%= viewBox %>'}
      width={props.width || '1em'}
      height={props.height || '1em'}
      style={{
        fontSize: props.fontSize || 'inherit',
        color: props.color || 'inherit',
        ...(props.style || {}),
      }}
    />
  );
}